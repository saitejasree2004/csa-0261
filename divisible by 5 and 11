#include<stdio.h>
int main()
{
	int number;
	printf("Enter a Number: ");
	scanf("%d",&number);
	
	if(number%5 == 0 && number%11 == 0)
	{
		printf("Number is divisible by 5 or 11");
	}
	else
	{
		printf("Number not is divisible by 5 or 11");
	}
	
	return 0;
}
